const AppConstants = {
    //Application name
    appName: 'Sphinks Token Wallet',

    version: '0.5.0',

    //Network
    defaultNetwork: 104,

    // Ports
    defaultNisPort: 7890,
    defaultMijinPort: 7895,
    defaultWebsocketPort: 7778,

    // Activate/Deactivate mainnet
    mainnetDisabled: false,

    // Activate/Deactivate mijin
    mijinDisabled: true,

    // Token issuance system (TIS) URL
    tis_url: "http://sphinks.org:8080"
};

export default AppConstants;
